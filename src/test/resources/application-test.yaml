server:
  port: 8082

spring:
  application:
    name: billing-test
  datasource:
    url: jdbc:postgresql://localhost:5432/billing_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: true
  test:
    database:
      replace: none